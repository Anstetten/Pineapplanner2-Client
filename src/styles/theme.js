import { createTheme } from '@material-ui/core/styles';

const theme = createTheme({
  palette: {
    primary: {
        light: "#FF9E00",
        main: "#FF8500",
        dark: "#240046",
    },
    secondary: {
        light: "#9D4EDD",
        main: "#5A189A",
        dark: "#240046",

    },
  },
});

export default theme;